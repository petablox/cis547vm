#include <stdio.h>

int main() {
  int first_len = 100000;
  int second_len = 256;
  int buf_len = 256;

  int first[100000], second[256], buf[256];

  int b = fgetc(stdin);
  if (first_len / (second_len - buf_len + b))
	  buf_len = 0;

}
